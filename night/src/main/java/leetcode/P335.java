package leetcode;

/**
 * 2021-10-29
 * 每日一题
 */
public class P335 {
    /**
     * 分两种情况？
     * 一种往里卷，一种往外卷
     * @param distance
     * @return
     */
    public boolean isSelfCrossing(int[] distance) {
        int min_bei=Integer.MAX_VALUE;
        int min_dong=Integer.MAX_VALUE;
        int min_nan=Integer.MAX_VALUE;
        int min_xi=Integer.MAX_VALUE;
        for (int i = 0;i < distance.length;i++){

        }
        return false;
    }
}
